{
  "identifier": "default",
  "description": "Default capability for Shelf",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "shell:allow-open",
    "pty:default",
    "dialog:default"
  ]
}
